import numpy as np
import pandas as pd
import pickle as pkl

def minibatches(data, mini_batch_num):
    X_train = data[:, 1:785].reshape(784, -1)
    print(X_train.shape)
    

if __name__ == "__main__":
    data = pd.read_csv('datasets/fashion-mnist_train.csv')
    data = np.array(data) # ( 60000, 785 ) 
    
    minibatches(data, 10)
